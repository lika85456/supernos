package nostale.packet.receive;

import nostale.packet.Packet;

//Skill restart packet - skill succesfully used
public class SrPacket extends Packet{

	public SrPacket(String str) {
		super(str);
	}
	
}
